const plus = (num1, num2, num3) => num1 + num2 + num3;
const multifly = (num1) => num1 * 12;
const name = (myname) => "Bibi Khadiza";
const name1 = () => "Bibi Khadiza";

const calculator = (x, y) => {
  const add = x + y;
  const minus = y - x;
  const multifly = x * y;
  const division = x / y;
  console.log(add, minus, multifly, division);
};

document.getElementById("example").addEventListener((event) => {});
