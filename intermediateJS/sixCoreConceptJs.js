// intermediateJS/sixCoreConceptJs.js

//1. variable
var deposite = 2332;

//2.  conditions
if (deposite < 300 && deposite != 3434) {
}

//3. array
const numbers = [21, 23, 232, 434, 44];
console.log(numbers.length);
console.log(numbers.pop());
console.log(numbers.push(111));
console.log((numbers[2] = 555));
console.log();

// check weather 222 included in the array
if (numbers.indexOf(222) != -1) {
}

if (numbers.indexOf(222)) {
  console(true);
}

// 4. loop
//while, for
for (const number of numbers) {
}

//5. function
function fullname() {
  const name = first + " " + localStorage;
}

const person = fullname("Abdur", "Rahaman");

//6,  object
const bottle = { color: "pink", contain: "water", price: 30 };
